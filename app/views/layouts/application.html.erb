<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= render 'layouts/default_head' %>
    <%= render 'shared/shim' %>
  </head>

  <body>
    <%= render 'layouts/header' %>
    <div class="content">
      <%= render 'users/flash' %>
      <%= yield %>
    </div>
  </body>
</html>
