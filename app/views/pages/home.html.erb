<%= provide(:button_text, "Sign In") %>
<% if signed_in? %>
  <ul class="list-items-collation">
    <% current_user.list_items do |item| %>
      <%= item.content %>
    <% end %>
  </ul>
<% else %>
  <h2>Sign In</h2>
  <%= render 'users/signin' %>
<% end %>
